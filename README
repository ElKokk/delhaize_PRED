usage: boosting_model.py [-h] --dataset DATASET --response RESPONSE [--epochs EPOCHS] [--test_size TEST_SIZE] [--output_dir OUTPUT_DIR]

Train and evaluate the model.

options:

  -h, --help            show this help message and exit

  --dataset DATASET     Path to the dataset CSV file.

  --response RESPONSE   Response variable to predict.

  --epochs EPOCHS       Number of trials for hyperparameter optimization.

  --test_size TEST_SIZE Number of observations to use for testing.
  
  --output_dir OUTPUT_DIR Directory to save output plots.








python boosting_model.py --dataset 'path_to_file' --response 'Ultrafresh' --output_dir 'folder path'
